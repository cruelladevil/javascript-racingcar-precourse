const INPUT_CAR_NAME = '경주할 자동차 이름을 입력하세요.(이름은 쉼표(,) 기준으로 구분)\n';
const INPUT_TRY_COUNT = '시도할 회수는 몇회인가요?\n';

const OUTPUT_START_RESULT = '\n실행 결과\n';

const ERROR_NAME_SIZE = '[ERROR] 차 이름은 5자 이하만 가능하다.\n'
const ERROR_NOT_STRING = '[ERROR] 차 이름은 문자여야 한다.\n';
const ERROR_NOT_NUMBER = '[ERROR] 시도 횟수는 숫자여야 한다.\n';

module.exports = {
  INPUT_CAR_NAME,
  INPUT_TRY_COUNT,
  OUTPUT_START_RESULT,
  ERROR_NAME_SIZE,
  ERROR_NOT_STRING,
  ERROR_NOT_NUMBER
}