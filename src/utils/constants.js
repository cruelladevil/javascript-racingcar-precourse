const RANDOM = Object.freeze({
  MIN_NUMBER: 0,
  MAX_NUMBER: 9,
  MOVE_CONDITION_NUMBER: 4,
});

const CAR_GAME = Object.freeze({
  MIN_PARTICIPANT: 1,
  MAX_NAME_LENGTH: 5,
  MIN_MOVE_COUNT: 1,
});

module.exports = {
  RANDOM,
  CAR_GAME,
};
